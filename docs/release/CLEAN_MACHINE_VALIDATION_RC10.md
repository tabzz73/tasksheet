# TaskSheet 1.0.0-rc.10 Clean-Machine Validation

In addition to RC9 validation:

- verify Settings has grouped vertical navigation without a horizontal settings-tab scrollbar on desktop;
- verify the narrow layout uses one Settings section selector;
- enter and save main, unit, and fax numbers from raw digits and verify `(AAA) BBB-CCCC` formatting;
- select every province/territory and verify the value saves correctly;
- verify city suggestions change by province and a custom municipality remains accepted;
- enter lowercase unspaced postal codes and verify uppercase `A1A 1A1` formatting;
- verify incomplete phone and postal values are rejected with clear messages;
- reopen Settings and verify saved contact/address formatting persists;
- navigate Print Profiles, Quick Add Presets, Attention & Safety, Catalog, Roles & Shifts, Preferences, Backup, and Demo Workspace from both desktop and narrow layouts.
