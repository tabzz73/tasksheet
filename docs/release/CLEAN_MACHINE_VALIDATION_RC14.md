# TaskSheet 1.0.0-rc.14 Clean-Machine Validation

Run the full clean-machine checklist against the exact checksummed RC14 installer. In addition to RC13 validation:

- verify “The Smartest Workflow for Modern Healthcare” appears in the desktop sidebar without clipping;
- verify the same tagline appears in App Information;
- verify Welcome and Presentation Mode use the same wording;
- verify the former “Shift Task & Print Center” subtitle is absent;
- confirm operational print document titles and clinical subtitles are unchanged.

Record the machine, Windows build, architecture, display scaling, installer SHA-256, Git commit/tag, screenshots, PASS/FAIL result, and all classified defects.
