# Build Evidence - TaskSheet 1.0.0-rc.25

The guarded tagged build generates `release/TaskSheet-Build-Evidence-1.0.0-rc.25.txt`.

Required gates: zero TypeScript errors, 151/151 tests PASS, production build PASS, clean tree, and exact `v1.0.0-rc.25` tag at HEAD.

The generated evidence is authoritative for the commit SHA, build timestamp, Node/npm versions, installer filename, installer SHA-256, and `dist/` hashes. Physical-print certification is a separate external gate.
