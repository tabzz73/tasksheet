# TaskSheet 1.0.0-rc.20 Clean-Machine Validation

In addition to RC19 validation, open three-dot menus on HCA resident and unit task rows at the top and bottom of cards. Confirm the complete menu is visible, flips upward when required, is scrollable in a short viewport, and closes on outside click, Escape, and scroll.
