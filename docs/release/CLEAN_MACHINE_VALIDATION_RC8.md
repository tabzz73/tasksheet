# TaskSheet 1.0.0-rc.8 Clean-Machine Validation

In addition to RC7 validation:

- generate an HCA sheet containing at least fifteen residents plus realistic unit tasks;
- verify compact portrait row spacing activates and all resident and unit tasks remain legible;
- physically print the HCA sheet on Letter portrait paper;
- verify optional handoff space reduces before operational rows are pushed to another page;
- add enough tasks to require page two and verify the table header repeats with no clipped or omitted rows;
- enable Spacious or Large Print and verify the explicit setting is preserved.
