# TaskSheet 1.0.0-rc.9 Clean-Machine Validation

In addition to RC8 validation:

- open the three-dot menu for residents at the top, middle, and bottom of the list;
- verify the complete menu remains visible and is not clipped by the list card;
- verify a bottom-row menu opens upward;
- verify the menu remains within the left and right window edges at common display scaling values;
- verify outside click, Escape, window resize, and list scrolling close the menu;
- execute View Full Profile, Move Room, and each Change Status action from the popup.
